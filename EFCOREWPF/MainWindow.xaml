<Window
    x:Class="EFCOREWPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EFCOREWPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <telerik:RadGridView x:Name="dg" AutoGenerateColumns="False">
            <telerik:RadGridView.Columns>

                <telerik:GridViewDataColumn
                    Width="auto"
                    DataMemberBinding="{Binding Title}"
                    Header="عنوان"
                    IsReadOnly="False" />
                <telerik:GridViewDataColumn
                    Width="auto"
                    DataMemberBinding="{Binding Des}"
                    Header="شرح"
                    IsReadOnly="False" />
                <telerik:GridViewDataColumn
                    Width="auto"
                    DataMemberBinding="{Binding Category.Title}"
                    Header="دسته بندی"
                    IsReadOnly="False" />

            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</Window>
